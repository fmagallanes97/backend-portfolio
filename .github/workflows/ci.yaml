name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: ./.github/workflows/launcher.yaml
    with:
      commands: test

  lint:
    name: SonarQube
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    uses: ./.github/workflows/launcher.yaml
    with:
      test
      testJacocoReport
      sonar -Dsonar.login=$SONAR_TOKEN
    secrets: inherit