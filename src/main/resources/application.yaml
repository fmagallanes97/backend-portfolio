spring:
  profiles:
    active: 'dev'
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false

server:
  port: 8080
  servlet:
    context-path: '/api/v1'

springdoc.swagger-ui.path: '/doc'