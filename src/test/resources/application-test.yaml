spring:
  datasource:
    url: jdbc:tc:mysql:8.0.31:///
  jpa.hibernate.ddl-auto: validate
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false

server:
  port: 8080
  servlet:
    context-path: '/api/v1'
